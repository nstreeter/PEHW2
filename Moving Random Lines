void setup() { 
size(500,500);
frameRate(2);
}

void draw() {
  background(0);
  int x=0;
int t=0;

while (t<1000){
  while(x<500) {
  float h =random(-450);
  
  noStroke();
  colorMode(HSB);
  fill(255);
  rect(x,500,5,h);
  x=x+15;
}
t=t+1;
}
}
